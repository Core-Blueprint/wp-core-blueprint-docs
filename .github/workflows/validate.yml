name: Docs checks

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: docs-checks-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: PHP ${{ matrix.php }} validation
    runs-on: ubuntu-24.04

    strategy:
      fail-fast: false
      matrix:
        php: ['8.4', '8.5']

    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Setup PHP and WP-CLI
        uses: shivammathur/setup-php@2.37.2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: wp-cli

      - name: Install gettext
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y gettext

      - name: Refresh i18n catalogs
        shell: bash
        run: bash tools/i18n/update

      - name: Upload refreshed i18n catalogs
        if: matrix.php == '8.4'
        uses: actions/upload-artifact@v4
        with:
          name: core-blueprint-docs-i18n
          path: languages/
          if-no-files-found: error
          retention-days: 1

      - name: Run Docs checks
        shell: bash
        run: bash tools/check

      - name: Upload validated plugin ZIP
        if: matrix.php == '8.4'
        uses: actions/upload-artifact@v4
        with:
          name: core-blueprint-docs-1.0.0-rc1
          path: |
            build/core-blueprint-docs-1.0.0-rc1.zip
            build/core-blueprint-docs-1.0.0-rc1.zip.sha256
          if-no-files-found: error
          retention-days: 14
